plugins {
  id 'java-library'
  id 'maven-publish'
}

group = 'com.git.bs'

eclipse.project.name = appName + '-core'

dependencies {
  api project(':common')
  api "com.badlogicgames.gdx:gdx:$gdxVersion"
  api "com.badlogicgames.gdx:gdx-freetype:$gdxVersion"   // BsSkinLoader 用 FreeTypeFontGenerator
  implementation "org.slf4j:slf4j-api:$slf4jVersion"

  compileOnly "org.projectlombok:lombok:$lombokVersion"
  annotationProcessor "org.projectlombok:lombok:$lombokVersion"
}

// 发布配置（发布到 mavenLocal；远程仓库见 gradle/publish.gradle 的注释段）
ext.publishArtifactId  = 'bs-ui-core'
ext.publishDescription = 'bs-ui: Bootstrap-style UI components for libGDX (core library)'
apply from: "$rootDir/gradle/publish.gradle"

// P1 spike：PakFileHandle 表面确定性检查（不依赖 GL）
tasks.register('pakSurfaceCheck', JavaExec) {
  group = 'verification'
  description = 'P1 spike: PakFileHandle 表面确定性检查'
  classpath = sourceSets.main.runtimeClasspath
  mainClass = 'com.git.bs.res.PakSurfaceCheck'
}
